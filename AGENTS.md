# AGENTS

## Branch awareness
- Always confirm the current git branch before editing version/menu strings (e.g., `git branch --show-current`).

## Shiny patch
- On the `shiny` branch, the main menu version label should read: "Shiny Patch v0.0.1".
- Update `include/shiny_version.h` when the Shiny patch version changes.
